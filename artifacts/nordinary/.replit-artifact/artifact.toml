kind = "web"
previewPath = "/"
title = "(n)ordinary Studios"
version = "1.0.0"
id = "artifacts/nordinary"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20459

[services.development]
run = "pnpm --filter @workspace/nordinary run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nordinary", "run", "build" ]
publicDir = "artifacts/nordinary/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20459"
BASE_PATH = "/"
